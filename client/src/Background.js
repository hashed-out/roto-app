import { View, Text, ImageBackground } from 'react-native'
import React, { Children } from 'react'


export default function Background({children}) {
  return (
    <View>
      {/* <ImageBackground source={require("../assets/bg2.jpg")} style={{height: '100%'}}/>
      <View style={{ position: 'absolute' , flex: 1, alignItems: 'center', justifyContent: 'center', color: 'white'}}>
      {children}
      
    </View> */}
    </View>
  )
}